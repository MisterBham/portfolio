import React from 'react';
import '../styles/header.css';
import Nav from './Nav';

function Header() {
    return (
        <header>
            <Nav />
        </header>
    );
}

export default Header;